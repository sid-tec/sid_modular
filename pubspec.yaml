name: sid_modular
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  firebase_core: ^2.13.1
  flutter:
    sdk: flutter
  flutter_modular: ^5.0.3
  sid_d_d:
    path: 'D:/DEVS/sid_d_d'
  secretaria:
    path: 'D:/DEVS/secretaria'  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/uff.PNG
    - assets/brasao.png
